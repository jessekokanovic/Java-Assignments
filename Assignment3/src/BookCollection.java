import javax.swing.JOptionPane;

public class BookCollection {
	
	Book[] bookCollection; //initialize the array to hold the book objects.
	
	public static void main(String[] args) {
		// TODO Auto-generated method stub
	}
	
	public BookCollection(){
		//A collection of Book objects stored in an array.
		
		//we need to set the length of our book array by reading how many lines are in the file and assigning the count as the length. 
		//this.bookCollection = new Book[];
	}

}
